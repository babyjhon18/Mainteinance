

@{
    Layout = null;
}


<div class="modal-dialog">
    <div class="modal-content">
        @using (Html.BeginForm("Create", "Jobs", FormMethod.Post, new { @class = "form-horizontal", @id = "CreateJobsForm", @role = "form" }))
        {
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h5 class="modal-title"><i class="fa fa-plus"></i> Добавить работу</h5>
            </div>
            <div class="modal-body">
                <fieldset>
                    <legend><i class="fa fa-pencil-square-o"></i>Работа</legend>
                    <div class="form-group">
                        <label for="JobName" class="col-lg-4 control-label">Наименование</label>
                        <div class="col-lg-8">
                            <input type="text" class="form-control" placeholder="Наименование" id="JobName" name="JobName" required />
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="JobTime" class="col-lg-4 control-label">Норма времени</label>
                        <div class="col-lg-8">
                            <input type="text" class="form-control" placeholder="Норма времени" id="JobTime" name="JobTime" required />
                        </div>
                    </div>
                </fieldset>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-sm btn-danger" data-dismiss="modal"><i class="fa fa-times"></i> Отмена</button>
                <button type="submit" class="btn btn-sm btn-primary"><i class="fa fa-plus"></i> Добавить</button>
            </div>
        }
    </div>
</div>

<script>
    $(document).ready(function () {
        $(".chosen-select").chosen({
            width: "100%",
            allow_single_deselect: true
        });

      $("#IsActive")
        .on("change",
          function() {
            if ($(this).is(":checked")) {
              $(this).val("true");
            } else {
              $(this).val("false");
            }
          });

    });
</script>